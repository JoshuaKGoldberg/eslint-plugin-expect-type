name: CI

on:
  push:
    branches:
    - master

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      name: npm install, build, test and publish
      run: |
        npm install
        npm run build
        npm publish
